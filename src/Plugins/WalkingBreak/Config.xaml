<UserControl
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WalkingBreak"
        xmlns:DBSettings="clr-namespace:Zeta.Bot.Settings"
        mc:Ignorable="d" Background="#393939">
    <StackPanel Orientation="Vertical" Margin="6">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="Key:" VerticalAlignment="Center" />
            <ComboBox Grid.Row="0" Grid.Column="1" x:Name="CB_KEY" ItemsSource="{x:Static local:Plugin.VirtualKeyNames}" SelectedValuePath="Value" Margin="4,0,0,0" Width="150" />
        </Grid>
    </StackPanel>
</UserControl>
